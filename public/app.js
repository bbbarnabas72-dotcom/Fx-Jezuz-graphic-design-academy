const $=s=>document.querySelector(s), site=$('#site');let data={};
async function api(path,opt={}){let r=await fetch(path,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});let j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'Request failed');return j}
async function load(){data=await api('/api/public');render()}
function render(){let s=data.settings,p=data.portfolio||[];site.innerHTML=`<section class="hero"><div><div class="eyebrow">PHYSICAL TRAINING · UDS TAMALE CAMPUS</div><h1>LEARN GRAPHIC DESIGN.<br>CREATE. GET PAID.</h1><p>Master practical graphic design through hands-on physical training at UDS Tamale Campus. Build a portfolio and learn how to turn creativity into income.</p><div class="actions"><a class="btn primary" href="#register">REGISTER FOR THE NEXT CLASS</a><a class="btn light" href="#work">VIEW MY WORK</a></div></div><div class="heroImg">${s.heroImage?`<img src="${s.heroImage}" style="width:100%;height:100%;object-fit:cover">`:'YOUR DESIGN WORK'}</div></section><section><div class="eyebrow">THE LOCATION</div><h2>TRAIN WITH US AT UDS TAMALE CAMPUS</h2><div class="grid"><article class="card"><div class="num">01</div><h3>Physical classes</h3><p>All lessons are conducted in person at the scheduled training location.</p></article><article class="card"><div class="num">02</div><h3>Hands-on practice</h3><p>Create real design projects while learning professional workflows.</p></article><article class="card"><div class="num">03</div><h3>Income focused</h3><p>Learn portfolio building, pricing, clients and how to monetize your skills.</p></article></div><p><b>${s.location}</b><br>${s.days} · ${s.time}</p></section><section id="classes" class="dark"><div class="eyebrow">NEXT AVAILABLE CLASS</div><h2>GRAPHIC DESIGN MASTERCLASS</h2><div class="classbox"><div class="meta"><div><small>Location</small><b>UDS Tamale Campus</b></div><div><small>Mode</small><b>Physical / In-Person</b></div><div><small>Duration</small><b>${s.duration}</b></div><div><small>Fee</small><b>GH₵${s.fee}</b></div><div><small>Level</small><b>Beginner to Advanced</b></div><div><small>Time</small><b>${s.time}</b></div></div><a class="btn primary" href="#register">REGISTER NOW</a></div></section><section id="curriculum"><div class="eyebrow">CURRICULUM</div><h2>WHAT STUDENTS WILL LEARN</h2><div class="grid">${['Graphic Design Fundamentals','Adobe Photoshop','Adobe Illustrator','Flyer & Social Media Design','Branding','T-Shirt Design','Getting Clients'].map((x,i)=>`<article class="card"><div class="num">0${i+1}</div><h3>${x}</h3><p>Practical lessons, exercises and real project work focused on building useful design skills.</p></article>`).join('')}</div></section><section id="work" class="dark"><div class="eyebrow">PORTFOLIO</div><h2>SOME OF MY WORK</h2><div class="workgrid">${p.map(w=>`<article class="work">${w.image?`<img src="${w.image}">`:'<div class="placeholder">DESIGN</div>'}<div class="caption"><small>${w.category||''}</small><h3>${w.title||''}</h3><p>${w.description||''}</p></div></article>`).join('')||'<p>No work uploaded yet.</p>'}</div></section><section><div class="eyebrow">4-WEEK SCHEDULE</div><h2>CLASS PLAN</h2><div class="schedule">${[['Week 1','Introduction to Graphic Design','Typography','Color','Composition'],['Week 2','Photoshop','Photo manipulation','Flyer design'],['Week 3','Logo design','Brand identity','T-shirt design'],['Week 4','Portfolio','Client management','Pricing','Getting clients']].map(x=>`<div class="day"><h3>${x[0]}</h3>${x.slice(1).map(y=>`<p>${y}</p>`).join('')}</div>`).join('')}</div></section><section id="register"><div class="eyebrow">REGISTRATION</div><h2>REGISTER FOR THE NEXT CLASS</h2><form class="form" id="regform"><div class="formgrid">${['fullName','phone','whatsapp','email','gender','age','programme','level'].map(x=>`<label>${x.replace(/([A-Z])/g,' $1')}<input name="${x}" ${x==='fullName'||x==='phone'?'required':''}></label>`).join('')}<label>Payment method<select name="payment"><option>MTN Mobile Money</option><option>Telecel Cash</option><option>AirtelTigo Money</option><option>Bank transfer</option><option>Cash payment</option></select></label><label class="full">Previous design experience<textarea name="experience" rows="4"></textarea></label></div><button class="btn primary">SUBMIT REGISTRATION</button></form></section>`;$('#regform').onsubmit=register}
async function register(e){e.preventDefault();let f=new FormData(e.target),b=Object.fromEntries(f);try{let r=await api('/api/register',{method:'POST',body:JSON.stringify(b)});e.target.outerHTML=`<div class="success"><h3>Registration Submitted</h3><p>Your registration number is <b>${r.registrationNumber}</b>.</p><p>Payment status: Pending</p></div>`}catch(x){alert(x.message)}}
$('#adminBtn').onclick=adminLogin;
async function adminLogin(){let u=prompt('Admin username','admin');if(!u)return;let pw=prompt('Admin password');if(!pw)return;try{await api('/api/login',{method:'POST',body:JSON.stringify({username:u,password:pw})});admin()}catch(e){alert(e.message)}}
async function admin(){let d=await api('/api/admin');site.innerHTML=`<div class="admin"><div class="adminbar"><div><div class="eyebrow">ADMIN DASHBOARD</div><h1>Academy Management</h1></div><button onclick="location.reload()">View Website</button></div><div class="stats"><div class="stat">Registered<b>${d.registrations.length}</b></div><div class="stat">Paid<b>${d.registrations.filter(x=>x.paymentStatus==='Paid').length}</b></div><div class="stat">Pending<b>${d.registrations.filter(x=>x.paymentStatus!=='Paid').length}</b></div><div class="stat">Revenue<b>GH₵${d.registrations.filter(x=>x.paymentStatus==='Paid').length*Number(d.settings.fee||0)}</b></div></div><div class="panel"><h2>Website Settings</h2><form class="adminform" id="settings">${['academyName','instructor','phone','whatsapp','email','location','fee','duration','days','time','status','heroImage'].map(k=>`<label>${k}<input name="${k}" value="${(d.settings[k]||'').replace(/"/g,'&quot;')}"></label>`).join('')}</form><button class="btn primary" id="saveSettings">SAVE SETTINGS</button></div><div class="panel"><h2>Add Portfolio Work</h2><form class="adminform" id="work"><label>Title<input name="title" required></label><label>Category<input name="category"></label><label>Image URL<input name="image"></label><label>Description<textarea name="description"></textarea></label></form><button class="btn primary" id="addWork">ADD WORK</button></div><div class="panel"><h2>Registrations</h2><div class="rows">${d.registrations.map(r=>`<div><span><b>${r.reg}</b> · ${r.fullName} · ${r.phone}</span><select onchange="pay('${r.id}',this.value)"><option ${r.paymentStatus==='Pending'?'selected':''}>Pending</option><option ${r.paymentStatus==='Paid'?'selected':''}>Paid</option><option ${r.paymentStatus==='Partially Paid'?'selected':''}>Partially Paid</option><option ${r.paymentStatus==='Cancelled'?'selected':''}>Cancelled</option></select></div>`).join('')||'<p>No registrations yet.</p>'}</div></div><div class="panel"><h2>My Work</h2><div class="rows">${d.portfolio.map(w=>`<div><span><b>${w.title}</b> · ${w.category||''}</span><button onclick="delWork('${w.id}')">Delete</button></div>`).join('')||'<p>No work yet.</p>'}</div></div></div>`;$('#saveSettings').onclick=async()=>{let b=Object.fromEntries(new FormData($('#settings')));await api('/api/settings',{method:'PUT',body:JSON.stringify(b)});alert('Saved')};$('#addWork').onclick=async()=>{let b=Object.fromEntries(new FormData($('#work')));await api('/api/portfolio',{method:'POST',body:JSON.stringify(b)});admin()}}
window.pay=async(id,status)=>{await api('/api/registrations/'+id,{method:'PATCH',body:JSON.stringify({paymentStatus:status})});admin()};window.delWork=async id=>{await api('/api/portfolio/'+id,{method:'DELETE'});admin()};load();
